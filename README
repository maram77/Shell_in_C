To compile and execute this binary:
1-Make sure the the shell file is executable by typing: "chmod +x compile.sh"
2-Execute the shell file to compile and generate the shell binary: "./compile.sh" 
3-Execute the shell binary by typing: "./shell"

Adding headers is optional, but it greatly improves our code readability, and prevents a few compiler warnings.